import { fromJS } from 'immutable';
import { } from '../../actions/<%= name %>/actions_types';

const INITIAL_STATE = fromJS({
    // add here your initalize states. eg: post: null
});

export default function (state = INITIAL_STATE, action) {
    switch(action.type) {

        default:
            return state;

    }
}